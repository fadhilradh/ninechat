# netlify-cli 18 needs Node 20+. Without this, an older Node fails deep inside
# the CLI with "SyntaxError: Unexpected reserved word" (top-level await) rather
# than saying which version it wanted.
engine-strict=true
